apiVersion: v1
kind: Service
metadata:
  name: postgres-service-simple
  namespace: n8n
  labels:
    app: postgres-simple
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: postgres-simple
